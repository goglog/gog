#!/usr/bin/env ruby
# -*- mode: ruby -*-

$LOAD_PATH << './lib'
require 'gog'

class Gog
  
  def self.start(args = ARGV)
    first_arg = args[0].downcase
    case first_arg
    when "version"
      $stdout.puts Gog.version
    end
  end

  self.start(ARGV)

end